import {DefaultLayout} from "../layouts";
import Profile from "../components/Profile";

const Other = () => {
    return (
        <DefaultLayout>
            <Profile />
        </DefaultLayout>
    );
};

export default Other;
